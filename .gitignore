# Copyrighted paper (not ours to redistribute) -- see README for citation instead
TAS/Tele_Assistance_A_Self-Adaptive_Service-Based_System_Exemplar.pdf

# Build output produced by TAS/TAS.v1.6/build.sh
TAS/TAS.v1.6/*/out/

# Stale precompiled output shipped in the original TAS.v1.6 distribution
TAS/TAS.v1.6/*/bin/

# Stale Ant/NetBeans build+deploy tree shipped in the original distribution
# (duplicated source, duplicated libs, compiled classes, dist jars, old JNLP files)
TAS/TAS.v1.6/TAS_gui/build/

# Eclipse workspace metadata (not per-project config -- large, personal, IDE-only)
TAS/TAS.v1.6/.metadata/

# macOS
**/.DS_Store
