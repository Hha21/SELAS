# Top-level Python env for this project (see .venv setup in PROJECT_PLAN.md / READMEs).
# Activate with: source .venv/bin/activate

# plotting and analysis for experiments/ (the controller has its own venv; see CONTROLLER/csf)
matplotlib
numpy
